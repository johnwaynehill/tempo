# Changelog

## 2026-09-18
- **Tempo for iOS: more Today widgets and Control Center controls.** A `systemLarge` Today widget (up to six tasks, plus an "Up next" section and a Complete button while running). A Lock Screen `accessoryCircular` complication: a live progress ring while a task's on the clock, otherwise a capacity gauge of today's completed count. An interactive Complete button next to the clock on the medium and large widgets, backed by a new `CompleteActiveTaskIntent` that mirrors Start Next's pattern exactly — the app's process when it's running, otherwise a standalone path that stops the shared timer, banks the run's minutes, and queues the completion for the app to send next launch. Two Control Center controls, Start Next Task and Complete Task, via `ControlWidget` — no capability gate beyond what App Intents already needed. "Complete Task" also joins the App Shortcuts list. Built while the user's paid Apple Developer Program enrollment processes (signed up today); push notifications, real Siri, and Apple Intelligence integration wait for that to clear.
- **Tempo for iOS: a Habits widget and an Add to Tempo capture widget.** The Habits widget (small and medium) lists today's habits with a tap-to-toggle circle per row and each row's current streak — a plain background `AppIntent`, so tapping never opens the app. Its instant feedback needed a new pattern: a tap records a short-lived (10-minute) override in a new App Group defaults key, which the widget's own timeline provider layers on top of the cached snapshot, since (unlike the timer) nothing outside the app can safely rewrite `cache.json` in place without risking a race with the app's own writes. Add to Tempo (small, plus Lock Screen rectangular and circular) deep-links straight into Inbox with the capture field already focused — widgets can't host a keyboard, so this is the fastest real capture one can offer, via a new `tempo://capture` link `TempoApp`'s `onOpenURL` picks up.

## 2026-09-17
- **Tempo for iOS, phase 4: Notes, Playlists, Plan My Day, Insights, Weekly Review, and Google Calendar connect.** A fifth "More" tab holds everything below the bottom four. **Notes** edits raw Markdown in place (no WYSIWYG on iOS), auto-derives project hashtags, and links a todo. **Playlists** get a detail screen with native reorder/delete, plus Start (pins the steps to Today) and Start & focus (pins them, then hands off to Focus Mode's existing walk-through). **Plan My Day** is the same five-step morning wizard as the web — check in, review yesterday, pick up to five tasks, set estimates, confirm — reusing the scoring and availability logic already in `TempoKit`. **Insights** and **Weekly Review** share a new `Insights` port of the web's stats aggregation (streaks, by-project breakdown, activity trend, on-time rate) plus hand-rolled SwiftUI bar/ring charts, since no charting library is available; Weekly Review adds the Time sense section (estimate vs. actual) and an autosaving weekly reflection. **Google Calendar** connect lives in a new Settings screen, using `ASWebAuthenticationSession` against a small server addition: `GET /api/google-calendar/connect` now accepts `?platform=ios`, and the OAuth callback redirects to the app's own `tempo://` URL scheme instead of the web app's `/settings` route when it does, since a free personal team has no Associated Domains for an https callback. Tempo AI chat was cut from this phase's scope, matching its removal from the web Today view. Built as a shared foundation (new `TempoClient` methods, the `Insights` logic port with its own tests, the nav shell) plus four parallel builds, one per screen; a review pass afterward caught the same bug in three of the four — `.onChange` used as a "user edited this" signal also fired on the initial server-side load, so opening a note, a weekly review, or a playlist silently wrote it straight back to the server — fixed by comparing against the loaded value first.

## 2026-09-15
- **Tempo for iOS, phase 3: widgets, Shortcuts, the share sheet, and mood in Apple Health.** **What a free team can sign**, checked against Apple's capability table because the app is signed with a free personal team: App Groups, Keychain Sharing and HealthKit yes; Siri, push and time-sensitive notifications no. So Shortcuts use App Intents and App Shortcuts, which need no Siri entitlement. **Shared foundation:** the API key and server address live in a team-wide Keychain group every target reads (a key stored by an earlier build migrates on launch); the App Group `group.com.johnwaynehill.Tempo` holds the offline cache (moved once from Application Support), the timer state, and an `Inbox/` folder where out-of-app writers drop one `WriteOp` file per write (TempoKit `PendingOpInbox`), which the app imports on launch, foreground and refresh. **Share extension:** text or a link from any app becomes an Inbox todo with a derived title and the link in the description; it saves directly with a 6 s timeout and falls back to the inbox, so an offline share still arrives exactly once. **Today widget** (small, medium, Lock Screen rectangular and inline) reads the shared cache and timer with no network; the medium size has a **Start next** button. **App Intents:** Start next task (a Live Activity intent that works with the app closed), Add to Tempo, What's on Today, and Start focus, all as App Shortcuts. **Mood:** a mood row on Today and a check-in sheet (the web's 1–100 slider) that logs to Tempo and, when "Also save to Apple Health" is on, writes a State of Mind sample (momentary emotion, valence mapped from 1–100, gentle labels per bucket). **Polish:** success haptics on every completion, light impacts on timer start/pause/resume, Today rows gain the web's complete circle, and a small sage sparkle that skips itself with Reduce Motion. **Server fix:** `POST /api/todos` is now idempotent on client-supplied ids (same user → 200 with the existing row; another user's id → 409). Before, a repeated create returned 500, and because the iOS sync queue keeps a 5xx write at its head, one retried create — a share whose timeout fired after the server saved it, or any lost response — would have blocked every later offline write. Built by three agents in isolated worktrees on top of a shared foundation, then merged and re-verified together. Verified on iOS simulators against the local API: Start next from a closed app started the timer and Live Activity and synced `startedAt`; Add to Tempo online and via the inbox with the server down; the widget gallery rendered all four sizes with real data on the merged build; a share saved online and, against a dead server, queued then synced exactly once; a mood saved to Tempo, and after tapping through Apple Health's permission sheet a State of Mind sample saved and read back (valence 0.80, happy + joyful); tapping a row's complete circle completed it without opening the todo; a repeated create now returns the original row. TempoKit at 78 tests; signed simulator build of app, widget and share targets clean. **Not verified:** a real Home Screen widget, a Siri phrase or Shortcuts run, and Safari's real share sheet (driven through debug hooks instead). **Known gaps:** a persistent 5xx other than a duplicate still stops the queue; titles that start with a date (`2026-01-01 …`) are rewritten by the server's body-date middleware; the share card view is also compiled, unused, into the widget.
- **Tempo for iOS gets a real app icon** — the sage-gradient "T." monogram on white from Figma "Tempo-Web", node `4:2` ("app-icon-editable 2"); the Home Screen icon had been blank. Sources are SVGs in `ios/Branding/AppIcon/`: the light icon is the Figma design as a full-bleed opaque square (iOS applies its own corner mask, so the Figma rounded base and clip are dropped); a **dark** variant keeps the geometry on a transparent background with the gradient lifted toward the web dark-theme primary (`#C9D6CF` → `#7DA393`); a **tinted** variant is grayscale on transparent for tinted Home Screens. The dark and tinted colors are a derivation, not in the Figma file. `node ios/scripts/generate-app-icon.mjs` renders all three to 1024 px PNGs with `sharp` (flattening the light one with no alpha channel, which iOS requires) into an `AppIcon` set with `luminosity` appearances. Note the web favicon and PWA icons in `public/` are the inverse design (sage background, cream monogram) and are unchanged. Verified: all three renders viewed at 1024 px and in a masked light/dark/tinted preview sheet; the light PNG reports no alpha; built with Xcode 27.0 for the iPhone 17 Pro simulator and the compiled `Assets.car` carries all three renditions (`AppIcon-Light`, `AppIcon-Dark` as `UIAppearanceDark`, `AppIcon-Tinted` as `ISAppearanceTintable`). Not verified on a Home Screen: the rebooted simulator put Tempo off the first page and the simulator control tool crashed before a swipe landed.
- **Tempo for iOS, phase 2: the daily loop, with the timer in the Dynamic Island.** `ios/` only. **TempoKit** gains `TimerState` (a value-type port of `useTimer`/`useTaskTimer`: first-start stamp, accumulated + running-since so elapsed is derived rather than counted, stale-day discard, `TaskTimer.stopResult`/`startPatch`/`stopPatch` for `started_at` and `actual_minutes`) and a **Store**: `CacheSnapshot` (todos, events, habits, projects, preferences, today sets) persisted as JSON, a `WriteOp` queue (create/update/complete/delete todo, toggle habit, set today set, update preferences) applied optimistically and replayed in order — 4xx drops the op, transport errors keep the rest — and `refreshToday` that pulls everything in parallel and re-applies queued ops on top. 73 tests / 11 suites (20 new, 158 new assertions). **App:** four native tabs (Today, Inbox, Backlog, Habits); `AppModel` renders the cached snapshot instantly, then refreshes, and every mutation goes through the queue with a non-blocking flush (also on foreground and pull-to-refresh); a one-line "Offline. Changes will sync when you're back." banner under the Today header when writes are pending offline. **Today** ports the Now card — summary line with the live projection and the overcommitment sentence, the running task lifted into a card with elapsed / estimate, a sage progress bar that stays full at 40% past the estimate, pause / stop / complete, and a play affordance on each row. The timer is a **Live Activity**: a widget extension (`TempoWidgets`) renders the Lock Screen banner and the Dynamic Island compact ring + clock, minimal clock, and expanded view; the app starts, updates and ends it on every timer transition. **Focus Mode** is a full-screen cover porting the web page: ring, Not now / Done / Skip, "Take a breath…" transition, break screen, peek-next, quick capture. **Inbox** opens with the capture field focused; **Backlog** sorts by the shared scoring and groups by project; **todo detail** edits title, description, energy, size, impact, estimate, project, due and reminder with debounced saves and status-aware actions; **Habits** toggles today's completion with haptics and shows the 7-day strip and streak. **Reminders** are scheduled local notifications from `reminderAt`, resynced after every load, so they fire with the app closed. The team ID now lives in `project.yml` so `xcodegen generate` keeps signing. Verified on the iPhone 17 Pro simulator against the local API as the dev user: a 40-second timed run completed from the Now card left `startedAt` and `actualMinutes: 1` on the server row; pause / resume / stop by hand; the Live Activity visible in the Dynamic Island with ring and clock after backgrounding; a capture made while pointed at a dead port showed the offline banner and reached the server on the next launch. `swift test` 73/73, `xcodebuild` clean for both targets.

## 2026-09-14
- **Tempo for iOS, phase 1: a native app you can run on your phone.** New `ios/` tree, nothing outside it touched. `ios/TempoKit` is a local Swift package (Swift 6, iOS 26 / macOS 26) with `Codable` models mirroring `api/src/db/schema.ts`, a `URLSession` client (`X-API-Key` auth, one method per endpoint, typed errors), and one-for-one ports of the pure logic the web keeps in `src/lib` — `TimeMath`, `Calibration`, `Availability`, `Recurrence`, `Scoring`, `Streaks`, and the Today-set resolution from `useTodaySet` — each with an injectable `now` and timezone. **53 tests / 262 assertions** under Swift Testing, ported line-for-line from the verification scripts that were run against the TypeScript, so the two implementations are provably in step; these are the first real tests Tempo has had. Two deliberate deviations, both fixes: monthly recurrence clamps to the target month (the TS `setMonth` overflows Jan 31 → Mar 31, skipping February) and day differences use `Calendar`, not ms ÷ 86400000, so DST doesn't shift due-date urgency. `ios/Tempo.xcodeproj` is generated from `ios/project.yml` with XcodeGen (`brew install xcodegen`, then `xcodegen generate`): one SwiftUI app target, iOS 26.0, strict concurrency, no third-party dependencies. Screens: sign-in (paste a `tempo_…` key from Settings → API Keys, verified against `GET /api/auth/me`, stored in the Keychain) and a read-only Today mirroring the web page — title and date, today's events, the "4 tasks · 2h 20m · done by ~7:01 PM" summary from `TimeMath`, task cards with chips, "Clear skies" empty state, pull to refresh, sign out. Design tokens from `index.css` live in `Theme.swift` with light/dark variants; fonts are system SF behind a `Theme.font` indirection until Manrope/Inter are bundled. `TEMPO_API_URL` and `TEMPO_API_KEY` environment overrides point a simulator at a local API without typing. Verified: `xcodebuild` for iPhone 17 Pro (iOS 26.5) clean; launched on the simulator against the local API as the dev user and Today rendered the same four tasks and summary line as the web app. Not yet run on a physical device — that needs a team picked in Xcode (see `ios/README.md`).
- **Server-side completion and today-set generation** — Phase 0 of `iOSApp.md`, and the first step of the native iOS client: the logic every client kept re-implementing now lives in the API. `POST /api/todos/:id/complete` (optional `actualMinutes`) sets the todo done and, for a recurring todo, creates the next occurrence as midnight in the user's `autoplanTimezone` via a new `api/src/lib/recurrence.ts` (a port of the web client's `getNextOccurrence`, anchored to an explicit IANA zone since the server runs in UTC). Until now only the browser advanced recurrence, so **a recurring todo completed through the MCP server never recurred** — fixed by moving MCP's `complete_todo` onto the endpoint. `POST /api/today-set/generate` runs the existing server scoring in `autoplan.ts` on demand (heuristic only, no AI spend, no status changes) with exactly the web client's rule — mandatory Chores first, then discretionary picks up to five minus pinned — and `useTodaySet` now calls it instead of running `suggestTodayTodos` in the browser on the first open of the day. While wiring that up: the first-open generation had been **dead since the Postgres migration** — `GET /api/today-set` answers a missing day with an empty default row carrying today's date, so the client's "date matches, already generated" check always passed and the set only ever came from the autoplan cron. The response now carries `exists: false` for a missing day and the client generates on that, so a fresh day without the cron gets suggestions again. `GET /api/todos` gains `?status=` and `?since=` filters so a phone can sync deltas. Verified: 14-case script against the real `recurrence.ts` (zone midnights in LA/PST/PDT/Tokyo, daily, weekly wrap and default, monthly clamp on the 31st and December rollover, DST end, mid-day instants); against the local API with the dev user: filters incl. 400s on bad values, a Mon/Wed/Fri todo completed on Monday produced Wednesday at 07:00Z with the parent link and rule carried over, idempotent second completion, 404, generate with and without a date; in the browser, Today on a fresh day called generate and rendered the set, and completing a row went through the new endpoint. `tsc` clean on frontend, `api`, `mcp`; no new lint findings. No schema changes.
- Commit `Docs/iOSApp.md` — the native SwiftUI iOS plan — with the decisions taken: native SwiftUI, pasted API key for v1, own-device distribution through Xcode.

## 2026-09-13
- Remove the Tempo AI chat bar from the Today view. It sat fixed above the bottom nav on every visit and rarely got used; Tempo AI is still one tap away in the menu, and Plan My Day covers the "help me plan today" case it was there for. Today loses the bar, its input state, and the extra bottom padding that only existed to clear it. Three open chat-bar polish items in `Future.md` are moot as a result. No API or schema changes.
- **Fix in-app AI being dead in production: Sonnet 4 is retired.** Every browser-side AI call (Tempo AI chat, pick-for-me, smart capture, completion toast) used `claude-sonnet-4-20250514`, and Anthropic now answers `404 not_found_error: model` for it — found while verifying the spend cap with a real call. Switched `AI_MODEL` to `claude-sonnet-5` (same tier, cheaper per token: $2 / $10 per million vs $3 / $15). Sonnet 5 runs adaptive thinking by default, which would have eaten the 60–120 `max_tokens` budgets of the one-line helpers and put a thinking block first in `content`, so every call now passes `thinking: { type: 'disabled' }` to keep behaviour as before, and the helpers read the first `text` block instead of assuming `content[0]`. No sampling parameters were in use, so nothing else in the request shape changes. The morning autoplan cron stays on `claude-sonnet-4-5`, which is still served. Verified with real calls: non-streaming and streaming replies from `claude-sonnet-5` through the proxy, and the chat UI in the browser.
- **Daily AI spend cap**, the last item on the API-hardening roadmap after rate limiting (#80) and key scoping (#85). Rate limiting bounds requests, not money: the proxy forwarded whatever body a caller sent, so a leaked `ai`-scoped key could set a 64k `max_tokens` and run roughly $30 a minute at the 30/min limit. Now every call through `POST /api/anthropic/v1/messages` is priced from the response's `usage` block (input, output, cache read/write, per-model rates in `api/src/lib/ai-pricing.ts`) and added to a new `ai_usage_daily` row for the user's local day (`autoplan_timezone`; migration `Docs/migrations/2026-09-13-ai-usage-daily.sql`, applied to prod by hand). Once the day's total reaches `AI_DAILY_CAP_USD` (default **$2**, about five times a heavy real day; `0` disables) the proxy returns `429` with an Anthropic-shaped `{ error: { type: "ai_budget_exceeded", cap_usd, spent_usd, resets_at } }`. One cap covers browser and API-key traffic alike. Two cheaper guards ride along in the same `aiBudget` middleware: the model must be one Tempo uses (400 otherwise, so a leaked key can't be pointed at a pricier model) and `max_tokens` is clamped to 4096. Streams are tee'd so the final `message_delta` usage is charged when they end; a stream that dies before `message_start` is charged an input-only estimate. The morning autoplan cron counts toward the same cap and falls back to the heuristic picker when over it. Client: Tempo AI chat shows "Tempo AI is resting until tomorrow" on that error instead of a raw message; the one-line helpers (pick for me, smart capture, completion toast) already fail quietly. Settings gains a **Tempo AI** section with today's spend against the cap, a sage progress bar, and the 7-day total, fed by new `GET /api/ai-usage`. Verified: script against `ai-pricing.ts` (cost math incl. cache multipliers, env parsing, model allowlist, SSE usage extraction from a real-shaped stream incl. abort before `message_start`, local-date and next-midnight across timezones); against the local API with the dev user: disallowed model → 400, a real 5-token call → row with 1 request and the right cost, a streaming call → output tokens recorded, a planted $2 row → 429 with the documented body, `GET /api/ai-usage` and the Settings section reflect it. `tsc` clean on `api` and frontend. **Also set a monthly spend limit on the Anthropic Console workspace as the backstop outside this code.**
- Fix playlist totals disagreeing with Today: a step without an estimate counted as 15 minutes on the Playlists and playlist detail pages but as 25 (or its size default) once started as a todo, so a playlist billed as "4 tasks · 1h" became 1h 40m on the Today summary line. Both pages now use the shared `getEstimate` from `src/lib/time.ts` — the same fallback the Now card, Focus Mode and Plan My Day use — so the number you see before pressing Start is the number you get after. Steps you add still get an explicit 15-minute estimate by default. `tsc` clean.
- **Playlists that play**, phase 4 of `TimeVisibility.md` and the last item on it. Playlist detail gains a primary **Start & focus** button next to Start: it pins the steps to Today as before, then opens Focus Mode with `?queue=<todo ids>` so Focus Mode walks exactly those todos in playlist order — with the timer, the transition pause and auto-advance it already has — instead of starting from whatever happens to be first on Today. Done steps drop out of the queue as they're completed; a timer running on some other task is stopped so the first step takes over. Without `?queue`, Focus Mode behaves as before. `usePlaylists.startPlaylist` now returns the created todo ids. Verified in the browser: created a three-step playlist for the dev user, pressed Start & focus → Focus Mode showed step 1 of 3 with the first step's title and timer, Done advanced to step 2 in order. `tsc` and `eslint` clean.
- **Overcommitment detection**, phase 3 of `TimeVisibility.md`. Settings → Preferences gains a **Working hours** row (two time inputs, default 09:00–17:00) stored as `work_day_start` / `work_day_end` on `user_preferences` (migration `Docs/migrations/2026-09-13-work-hours.sql`, applied to prod by hand before deploy — not `db:push`). New pure module `src/lib/availability.ts` computes the free time between `max(now, work_day_start)` and `work_day_end`, subtracting today's timed calendar events clipped to that window with overlaps merged; all-day events are not blocking (they're stored at noon UTC since the 2026-06-17 fix, so their times must not be read as busy blocks). When the minutes still planned exceed the free time by more than 30 minutes, one sentence appears in `text-on-surface-variant` — "About 1h 30m planned, and roughly 30m free before 12:22 PM. Want to move something to tomorrow?" — under the Today summary line and under "Done by ~" on Plan My Day's confirm step. Nothing is shown when the plan fits, when the gap is within rounding noise, or once the work day is over. On Today the planned figure uses what's left of the running task's estimate, so the note clears as work gets done or deferred. Malformed hour strings fall back to 9–5 rather than breaking the page. `GET /api/preferences` defaults and `Docs/API.md` include the new fields. Verified: 27-case script against the real `availability.ts` exports (window before / during / after hours, overlap merging, all-day exclusion, clipping at both window edges, the 30-minute threshold on both sides, after-hours suppression, fully-booked wording, nearest-5 rounding in copy, today-only event filtering, timer narrowing); in the browser, set the end time to 30 minutes out via the Settings inputs, gave the one Today todo a 90-minute estimate → the sentence appeared on Today with the right numbers, and again on Plan My Day's confirm step. `tsc` clean on frontend and `api`; `eslint` clean on touched files.

## 2026-09-12
- **Remember how long things took**, phase 2 of `TimeVisibility.md`. Two new nullable columns on `todos` (`started_at`, `actual_minutes`; migration `Docs/migrations/2026-09-12-todo-actual-minutes.sql`, applied to prod by hand before deploy — not `db:push`). The first time a timer starts on a todo it stamps `started_at`; stopping the timer or completing the todo adds the run to `actual_minutes` (runs under 30 s are ignored as accidental taps, otherwise rounded to a whole minute, minimum 1). Completing from the Now card or Focus Mode writes the minutes in the same `PUT` as the status change; a plain Stop writes them on their own. All of that lives in a new `useTaskTimer` hook wrapping `useTimer`, so the pages don't know about the bookkeeping. **Weekly Review gains a "Time sense" section** — hidden until the week has at least three timed completions, since fewer than that says nothing — with one neutral headline ("Tasks ran about 27% longer than you guessed" / "Your estimates were close this week"), paired estimate-vs-actual bars by size and by top-3 project, and a single pattern line when a size or project ran ≥1.3× over on three or more tasks. Untimed completions are excluded rather than guessed. The estimate chips in Plan My Day and the todo detail page now show "Medium tasks usually take you about 45m." from the last four weeks of timed work for that size. **Stale overnight timers are gone:** the timer now records its first-start timestamp and discards itself on load or on the next tick if that was a different calendar day, so yesterday's forgotten 23-hour run no longer greets you on Today (the new state shape also drops any pre-existing persisted timer once). MCP `complete_todo` accepts an optional `actual_minutes`; `list_todos` prints `actual: Nm` when set. Verified: 25-case script against the real `calibration.ts` / `time.ts` exports (timed/untimed/zero filtering, threshold, size ordering, default-estimate fallback, headline bands, pattern fall-through to project, 4-week window, nearest-5 rounding, run rounding); in the browser, started a task, let it run 35 s, completed from the Now card → `started_at` and `actual_minutes: 1` on the row via the API; Weekly Review rendered the section over five timed completions with the correct bars and pattern line; a hand-planted timer stamped yesterday disappeared on reload. `tsc` clean on frontend, `api`, and `mcp`; `eslint` clean on the touched files.

## 2026-09-11
- Bring the timer back to the Today page as a **Now card**, the first phase of the time-visibility plan in `TimeVisibility.md`. Since April the only place time was visible was Focus Mode; the old `TimerBar` had been removed from Today and left as dead code. Today now shows one quiet summary line above the list — "3 tasks · 1h 30m · done by ~5:25 PM" — recomputed every second, with a Start button until a timer is running. Starting a task (from that button, or the play affordance that appears on any row) lifts it out of the list into a card with elapsed / estimate, a thin sage progress bar that stays full past the estimate rather than turning red, pause / stop, and the same sparkle-complete control as a list row, so the task is never shown twice. The projection is now correct while running: the active task contributes only what's left of its own estimate (floored at 0, so running over on one task no longer eats into the rest), where the old bar subtracted elapsed from the day's total. That math, plus `getEstimate` (previously copy-pasted in four files) and the format helpers, moved into a pure `src/lib/time.ts`; Plan My Day's "Done by" uses it too. `useTimer` was rebuilt on wall-clock timestamps instead of a counting interval — elapsed is derived from `accumulatedSeconds + (now − runningSince)` so a throttled or backgrounded tab can't lose seconds — and persists to `localStorage` instead of `sessionStorage`, so closing the PWA no longer kills a running timer; `stop()` now returns the final elapsed seconds for the next phase (actual-minutes tracking). Two tabs stay in sync via the `storage` event. Focus Mode now follows whichever task the timer is on (it always opened on the first todo before, which would have shown the wrong title with the running task's clock), and its "← Today" link no longer stops the timer. Verified: 19-case script against the real `time.ts` exports (no timer, mid-task, over-estimate, ceil-to-minute, active id not in list, single/zero todos, formatters); in the browser, started a task from a row's play button, reloaded (elapsed continued 0:04 → 0:07), paused (label + frozen clock), resumed, opened Focus Mode (same task, "2 of 3", same clock), returned to Today (still running), completed from the card (toast, list shrank, summary and Start button returned). `tsc -b` and `eslint` clean on the touched files. No API, schema, or env changes.

## 2026-08-25
- Make **overdue Chores always show up on Today**, no matter how full the day already is. The 2026-07-01/07-02 fixes gated Chore-project todos to their *literal* due date — eligible only when `due == today`, excluded before **and after** — on the theory that a chore whose date passed should stop resurfacing until rescheduled. In practice that's backwards for the one case that matters: miss "Clean kitchen" on its day and it silently vanishes from Today the next morning, which is exactly how it stays undone. The gate is now one-sided: still never suggested *before* the due date (no crowding the pool in the run-up), but eligible from that date onward and every day after until it's actually done. Overdue chores keep the existing **mandatory** treatment — force-included in the final picks rather than left to compete on score, and additive on top of the 3–5 target, so they're never dropped even when Today is already full of pinned work. Several outstanding chores sort oldest-due-first, so the most overdue one leads. Mirrored in both `api/src/lib/autoplan.ts` (server cron) and `src/lib/scoring.ts` (client fallback / Plan My Day); the constant is renamed `DUE_DATE_ONLY_PROJECTS` → `DUE_DATE_GATED_PROJECTS` in both, since "only on the due date" no longer describes the rule. Verified by running the real `suggestTodayTodos` against constructed cases: overdue chore included, due-today included, due-tomorrow and no-due-date still excluded, case/whitespace project variants matched, done and dismissed-today chores still excluded, and 7 overdue chores all survive a limit of 5 with 5 slots already pinned. `tsc --noEmit` clean on both the frontend and `api`. No schema, env, or UI changes.

## 2026-07-21
- Fix `/design-system` redirecting to Today for anyone with Tempo's PWA service worker installed (i.e. any real returning user, including the person who filed this bug). The server was already correct — verified via `curl`, `200` with the right title and byte count — so this was invisible to server-side testing. The actual cause: `vite-plugin-pwa`'s default `generateSW` setup installs a catch-all `NavigationRoute` that serves the cached app-shell `index.html` for *every* browser navigation once the service worker is active, including `/design-system`. Once that loads, React Router doesn't recognize the path and its `path="*"` catch-all bounces to `/today` — the exact symptom reported, and a client-side effect the previous `/design-system` fix (2026-05-22) never exercised since it was verified without a real installed service worker in the loop. Fixed with `navigateFallbackDenylist: [/^\/design-system/]` in `vite.config.ts`'s workbox config, excluding that path from the SPA fallback so the SW lets the request reach the network. **Verified with a real installed, active service worker** (not just a compiled-output check): served the production build locally, registered the SW for real, navigated to `/design-system` in that session, and confirmed the URL held, the title read "Tempo · Design System", and no React root mounted — the static page, not the SPA. **Existing installs need a service-worker update to pick this up** — click "Check for updates" in Settings (or reload after Tempo's own update prompt appears) once this deploys; a fresh service worker install (e.g. clearing site data) also gets it immediately. No server, schema, or env changes.

## 2026-07-02
- Fix a gap in yesterday's Chore-todo autoplan fix: a Chore due today would correctly become *eligible* for Today, but nothing guaranteed it actually got picked — the AI/heuristic ranking pass was still free to fill all 3–5 slots with other candidates and leave it out. That's exactly what happened this morning: "Clean kitchen" (due 7/2) stayed in `backlog` while 4 unrelated todos were pinned. Due-today items in a `DUE_DATE_ONLY_PROJECTS` project are now **mandatory**, not just eligible — unioned into the final picks after the AI/heuristic pass runs, additive on top of the 3–5 target (so Today can occasionally run over 5 on a day with more than one thing due). Same fix mirrored in both `api/src/lib/autoplan.ts` and the client fallback `src/lib/scoring.ts`. Verified against a reconstruction of this morning's exact failure (same 4 real todo IDs + the real "Clean kitchen" chore) — the mandatory union now force-includes it, placed first, with no duplicate if it happened to already be picked. Manually pinned today's "Clean kitchen" to `today_pinned` directly (surgical fix for right now, not a full re-run that would've reshuffled the other 4 already-decided picks) so today's Today view reflects the intended behavior without waiting for tomorrow's cron.

## 2026-07-01
- Fix autoplan suggesting Chore-project todos on the wrong day, and crowding out real priorities. Chores (recurring cleaning tasks) were scored the same as everything else, so they'd start earning strong due-date-urgency points up to a week before their due date and — worse — kept scoring at the **maximum** urgency forever once overdue, since `daysUntil < 0` also returns `1.0`. With ~10 rotating Chore todos, that meant they routinely crowded the 12-candidate pool fed to the AI/heuristic picker, pushing out genuinely high-priority work and making the Today list easy to ignore. Chore-project todos (`Chore`, matched case-insensitively) are now a hard candidacy gate in both the server autoplan (`api/src/lib/autoplan.ts`) and the client fallback (`src/lib/scoring.ts`): eligible **only** on their literal due date, excluded entirely otherwise — not just deprioritized. Also fixes a latent bug in the server-side date math: due-date-urgency comparisons used the API server's local timezone (UTC on Railway) instead of the user's configured `autoplanTimezone`, which could misjudge which calendar day a due date "counts as" near timezone boundaries; day comparisons are now anchored to the user's IANA timezone throughout. Verified against production data (20/20 assertions, incl. every real Chore todo's actual due date) and a clean `tsc` build on both `api` and the frontend; no schema or env changes, no UI change. `DUE_DATE_ONLY_PROJECTS` is a small constant in each file — easy to extend if another project needs the same "exact day only" rule.

## 2026-06-17
- Fix all-day Google Calendar events landing a day early. Google sends all-day events as a bare date (no time/zone); the sync parsed it as `…T00:00:00`, which on the UTC API server became midnight UTC — and the frontend, which buckets/displays events by the viewer's local date, rendered it a day earlier in any Americas timezone (e.g. a Monday event showing on Sunday). All-day events are now anchored at **noon UTC** (`…T12:00:00Z`), keeping the local calendar date correct from UTC-12 through UTC+11. Timed events were unaffected. **Existing mirrored all-day events correct themselves on the next sync** (hit "Sync now" or wait for the twice-daily cron).
- Left-align the Today view's mood check-in and event rows with the page's heading and section labels. The "How are you feeling?" button carried `px-4` and the event rows `px-2`, so their content sat indented from the heading/labels. Both now use a `-mx-2 px-2` negative-margin trick: content lands flush with the heading's left edge while the hover highlight keeps its inset (verified — heading, date, section labels, mood icon, and event icons all share one left edge).
- Make the Today view's event sections visually distinct from todos. Events were white rounded cards just like todo items, so they read as tasks. They now render **flat** (no card) on the page: today's events lead with a calendar icon, "Next 3 days" keeps its day chip, and rows still tap through to the calendar. Todos remain the only card-shaped, white elements — restoring a clear "cards = do, flat = reference" hierarchy. The count badge, sorting, inline location, and empty placeholder are unchanged.
- Remove the "Google" tag from calendar events in the Backlog/calendar day-detail panel. The source badge added in the Phase 3 read-only treatment is gone everywhere; Google-mirrored events still render read-only (no click-to-edit, no delete) — just without the label.

## 2026-06-16
- Add calendar event sections to the Today view. A compact **Today's events** strip sits above the task list (sorted all-day-first then by time, with a count badge and inline location), and a **Next 3 days** feed (day-chip rows) sits below — keeping tasks the visual focus. Events are source-agnostic (native Tempo + mirrored Google events alike, no source chip). Tapping any event opens that day on the calendar (`/backlog?view=calendar&date=…`); `CalendarView` gained an `initialDate` prop and Backlog reads a `date` param to focus the day. Empty today shows a quiet "No events today" placeholder; the upcoming section hides entirely when there's nothing in the next 3 days. New `src/components/today/dayEvents.tsx`.
- Preventatively fix the `autoplan-cron` Dockerfile the same way as `gcal-sync-cron` — `COPY run.sh …` → `COPY api/cron-autoplan/run.sh …`. The bare path only builds when the context is the subfolder; with Railway Root Directory `/` (as its committed `railway.json` implies) a rebuild would fail with `"/run.sh": not found`. The running service is unaffected until its next rebuild; this just makes that rebuild safe. No behaviour change.
- Fix the `gcal-sync-cron` Dockerfile so it builds with Railway's default Root Directory (`/`). Its `COPY run.sh …` resolved against the repo-root build context and failed (`"/run.sh": not found`) because `run.sh` lives in the `api/cron-gcal-sync/` subfolder. Changed it to the full repo path `COPY api/cron-gcal-sync/run.sh …`. With Root Directory `/` and the Config-as-Code path set to `api/cron-gcal-sync/railway.json`, the build resolves both the Dockerfile and the script.
- Docs: fix the `gcal-sync-cron` setup README. It omitted the **Root directory: `/`** step, so following it could land the railway.json path in Railway's Root Directory field, failing the build with `stat .../api/cron-gcal-sync/railway.json: not a directory`. Clarified that the repo root is the build root (the `railway.json` path goes in the Config-as-Code field), matching `cron-autoplan`, and added a troubleshooting note for that exact error.
- Refactor: de-duplicate the calendar components shared by the Calendar page and the Backlog calendar widget. `DayDetailPanel`, `EventFormModal`, and the shared `EVENT_COLORS`/`WEEKDAY_HEADERS` constants were copy-pasted verbatim in both `src/pages/Calendar.tsx` and `src/components/backlog/CalendarView.tsx`, so every event-rendering change (e.g. the Phase 3 read-only Google event treatment) had to be made twice. Extracted the identical inner pieces into a new `src/components/calendar/` module (`DayDetailPanel.tsx`, `EventFormModal.tsx`, `constants.ts`); both files now import them. Page-level differences are kept inline — the Calendar page keeps its page header + loading state and full-size month nav, the Backlog widget keeps its compact inline month nav. No behaviour change: the day grid, day selection, event form modal, and read-only `source='google'` styling (badge, distinct dot, no click-to-edit/delete) render identically. `npx tsc -b` clean.

## 2026-06-13
- Google Calendar sync — **Phase 4: scheduled cron** (completes the feature). New internal endpoint `POST /api/internal/google-sync` runs `syncAllEnabledUsers()` for every connection with `sync_enabled = true`; guarded by a shared secret (`X-Google-Sync-Secret` / `GOOGLE_SYNC_SECRET`), disabled-by-default (503 when unset), mounted outside the `/api` auth chain — same pattern as autoplan. New `api/cron-gcal-sync/` Railway cron service (clone of `cron-autoplan`) POSTs to it **twice daily** (`0 13,1 * * *` → ~06:00 and ~18:00 Pacific). **Deploy steps:** set `GOOGLE_SYNC_SECRET` on `tempo-api`, and provision the `gcal-sync-cron` Railway service per `api/cron-gcal-sync/README.md` (set `API_URL` + `GOOGLE_SYNC_SECRET` on it). No DB or schema changes.
- Google Calendar sync — **Phase 3: front-end UI**. Adds a **Google Calendar** section to Settings: a Connect button (redirects to Google consent), and once linked, the connected account email, last-synced time, a **Sync now** button, and **Disconnect**. The page reads the `?google=<status>` OAuth callback param to show a toast (connected / denied / expired / error), then strips it from the URL. On the calendar, `source='google'` events now render **read-only** — a "Google" badge, a distinct dot color, no click-to-edit and no delete button (matching the API's 403 guard) — in both the Calendar page and the Backlog calendar view. Adds `api.googleCalendar.{status,connect,sync,disconnect}` to `src/lib/api.ts` and `source`/`external_id` to the `CalendarEvent` type. No backend or env changes.
- Google Calendar sync — **Phase 2: sync engine** (one-way Google → Tempo). Mirrors the user's **primary** Google calendar into `calendar_events` as read-only `source='google'` rows over a rolling **−7 to +90 day** window. New `api/src/lib/google-calendar.ts` does a full window re-list each run (recurring events expanded via `singleEvents=true`) and reconciles — upsert by `(user_id, external_id)`, then prune any `source='google'` rows not returned (covers deletions, cancellations, and events aged out of the window); idempotent, with automatic access-token refresh. New `POST /api/google-calendar/sync` triggers a sync on demand (background cron lands in Phase 4). `calendar_events` gains `source`/`external_id`/`etag` columns + a `(user_id, external_id)` unique index for upserts (native events stay `source='tempo'`, `external_id` NULL). The events routes now reject edits/deletes to `source='google'` rows (`403`) and ignore client-supplied `source`/`external_id`/`etag`. Disconnect now also purges mirrored events. **Deploy step:** apply `Docs/migrations/2026-06-13-google-calendar-event-source.sql` (additive; do **not** use `db:push` on this project). No new env vars.
- Fix schema drift on the `projects` table — apply the missing `projects_user_id_name_unique` constraint (`UNIQUE (user_id, name)`) to production. The constraint has been defined in `api/src/db/schema.ts` (`unique().on(table.userId, table.name)`) but was never applied to the production Postgres DB; the drift was discovered on 2026-06-09 when `db:push` offered to TRUNCATE the table to reconcile it. Verified zero duplicate `(user_id, name)` rows in production first (no dedupe needed), then applied via a targeted, idempotent migration (`Docs/migrations/2026-06-13-projects-user-name-unique.sql`) rather than `db:push`, which would have bundled unrelated drift into one destructive interactive sync. No app code change — schema already matched; this only reconciles the live database.

## 2026-06-09
- Google Calendar sync — **Phase 1: OAuth account link** (one-way Google → Tempo). Lays the backend foundation for mirroring a user's Google Calendar into Tempo's calendar as read-only events; this phase establishes the account connection only (no event import yet). New `google_calendar_connections` table stores per-user OAuth tokens, **AES-256-GCM encrypted at rest** via `api/src/lib/crypto.ts` (`TOKEN_ENCRYPTION_KEY`). New `api/src/lib/google-oauth.ts` wraps Google's OAuth/OIDC endpoints with plain `fetch` (no `googleapis` dep, matching the Anthropic integration). New routes in `api/src/routes/google-calendar.ts`: `GET /status`, `GET /connect` (returns consent URL), `DELETE /` (revoke + drop), and a **public** `GET /callback` mounted before the `/api` auth chain (Google redirects there without an auth header; a one-time 10-min `state` row in `mcp_oauth_state` ties it back to the user). Integration is disabled-by-default: returns `503` / redirects with `?google=error` unless `GOOGLE_CLIENT_ID`/`GOOGLE_CLIENT_SECRET`/`GOOGLE_OAUTH_REDIRECT_URI` and `TOKEN_ENCRYPTION_KEY` are set. **Deploy steps:** run `npm --prefix api run db:push` (or apply `Docs/migrations/2026-06-09-google-calendar-sync.sql`); create a Google Cloud OAuth client and set the env vars per `Docs/GoogleCalendarSync.md`. Later phases add the sync engine, read-only calendar rendering, and a twice-daily cron.
- Fix mobile Toast (`CompletionToast`) layout. The toast button hugged its text content capped at `max-w-[90vw]`, so on narrow phones a medium-length message hit the cap and wrapped to two lines while the toast never used the full width. The wrapper now spans edge-to-edge with even 1rem side margins on mobile (`left-4 right-4`) and reverts to centered (`sm:left-1/2 -translate-x-1/2`) on ≥640px; the button is `w-full` with centered content on mobile and a content-hugging `sm:w-auto sm:max-w-[90vw]` pill on desktop.

## 2026-05-22
- Fix design system page rendering blank in production; switch its URL to the cleaner `/design-system` (was `/design-system.html`). The static server's `cleanUrls` redirect + SPA catch-all combo was stripping `.html` and then serving the React app's `index.html` for a path React Router doesn't have. Folder-with-index layout + explicit rewrites in `public/serve.json` (replacing the Dockerfile's `-s` flag) fixes it. Legacy `/design-system.html` bookmarks still redirect to the clean URL.
- Docs: correct root cause for the `nightly-tempo-plan` autonomous-run failure in `Docs/ClaudeRoutines.md`. The 7-second early termination wasn't a harness session-kill bug — it was an un-approved tool permission prompt that scheduled-task sessions can't service unattended. Section renamed to *First-run setup: pre-approve tool permissions* with the actual fix (run the routine once interactively and click "Always allow"). The *Adding a new routine* section now also reminds operators to do a manual first-fire after creating any new routine.
- Docs: post-autoplan doc updates. `Docs/API.md` adds the new `autoplanEnabled`/`autoplanTimezone` fields to the Preferences endpoint and documents `POST /api/internal/autoplan` under a new *Internal endpoints* section. `Docs/Future.md` marks server-side morning auto-plan (#92) + nightly Claude Routine (#91) as built under *Tempo AI*. `Docs/ProjectReference.md` adds `autoplan-cron` to the Hosting row, points to `api/cron-autoplan/` in the repo tree, and mentions autoplan fields on `user_preferences`. `Docs/ClaudeRoutines.md` gains a *Known issue* callout for the harness terminating scheduled-task sessions before tool results return — observed twice and documented with a workaround.
- Fix deferred todos still showing on Today view. The `useTodaySet` hook resolves the morning `today_set` snapshot to live todo objects but only filtered out `done` todos at render time — so a todo deferred via the Todo Detail page (which doesn't call `dismissFromSet`) stayed in `today_set.todo_ids` and re-rendered on Today with its new `deferred` status. The bug was latent before PR #92 (server-side auto-plan) since most days the `today_set` row was empty; now that auto-plan populates it every morning, the gap is reliably hit. Filter now only keeps `today_pinned` and `backlog` statuses — drops `deferred`, `inbox`, and `done`.

## 2026-05-21
- Server-side morning auto-plan. A new Railway cron service (`autoplan-cron`) hits `POST /api/internal/autoplan` daily at 13:30 UTC (06:30 PT) and, for every user with `autoplan_enabled = true`, replaces their Today view with 3–5 AI-picked todos. Picks reuse the same heuristic-scoring algorithm as the in-app planner (`src/lib/scoring.ts`) ported to the API, with an Anthropic ranking pass over the top 12 candidates to keep the morning load coherent (energy match + due-date urgency + variety). Heuristic-only fallback ships if Anthropic is unreachable. Endpoint is guarded by `AUTOPLAN_SECRET` (returns 503 when unset, never open-by-default) and is idempotent within a 24h window per user. New Settings toggle + timezone field under Preferences. Migration adds `autoplan_enabled`, `autoplan_timezone`, `autoplan_last_run_date` to `user_preferences`. **Deploy step:** run `npm --prefix api run db:push` (or apply `Docs/migrations/2026-05-21-morning-autoplan.sql`) before deploying, set `AUTOPLAN_SECRET` on the `tempo-api` service, and follow `api/cron-autoplan/README.md` to provision the new cron service. Closes the "Have Tempo AI run every morning and automatically update the Today view" and "Auto-plan Today with Tempo AI at a set schedule" bugs.
- Add nightly Claude Routine `nightly-tempo-plan` that fires at 9:00 PM Pacific every day, reviews the user's backlog + inbox via the Tempo MCP, and writes a `Plan for <tomorrow>` Tempo Note with 3–5 picks and short per-todo implementation plans. New `Docs/ClaudeRoutines.md` documents the routine and how to edit/disable it via `mcp__scheduled-tasks__*` tools. Closes the "Schedule Claude to run every night to check Tempo ToDos and write a plan for me to approve" bug (todo `d947e253-f1aa-4920-a6f2-1d5966198b03`).
- Backlog filter persistence + Esc-to-close on Todo detail. The Backlog page now syncs its energy/project/sort/view state to URL search params, so opening a todo and clicking Back restores the exact filtered view (and the URL is now bookmarkable/shareable). Adds an Esc-key shortcut on the Todo detail page that navigates back to the previous view; if a modal/menu is open, Esc closes that first via capture-phase listeners with `stopImmediatePropagation`, so it takes two Esc presses to leave a modal-open detail page. Closes the "Updating a ToDo from a Filtered view should return back" and "Esc shortcut to close ToDo detail" bugs.
- Fix Repeat schedule and Set reminder on Todo detail — both pickers were anchored via `position: absolute` to a trigger element that no longer existed (they're now invoked from the overflow menu), so they rendered off-screen. Reworked both as bottom-sheet on mobile / centered modal on desktop, matching the new `ProjectPicker` pattern. Reminder's custom date picker now uses `DateField`, inheriting the iOS form-field fixes from the previous PR. Closes the "no way to set Repeat schedule" and "Set reminder no longer works" bugs.
- Mobile UX overhaul for Project + Due date inputs on the todo capture/edit flows:
  - **Quick-capture drawer** now uses `ProjectChips` — flat one-tap chips for existing projects instead of an anchored typeahead. Tap a chip to assign, tap again to clear. No menu, no keyboard fight.
  - **Todo detail page** now opens a bottom sheet (mobile) / centered modal (desktop) when picking a project — replaces the anchored popover that fought iOS Safari over positioning. Search input inside, "Create new" option when query doesn't match, ↑/↓/Enter/Esc keyboard nav.
  - **`DateField` component** replaces every `<input type="date">` in the todo forms. Renders a styled button with a transparent native input layered on top, so iOS Safari opens its native picker on tap while we control every visible pixel. Adds an explicit `showPicker()` call on click for Firefox/Chrome desktop, which don't auto-open from a click on the input element.
  - Global CSS rule forces `font-size: 16px` on `input`/`textarea`/`select` under 768px, replacing the per-input `text-base` patch from #31 that kept regressing. Prevents iOS Safari from auto-zooming on focus.
  - `color-scheme: light` / `dark` on `:root` / `[data-theme='dark']` so native form controls (date placeholders, calendar icons, scrollbars) render correctly in both themes — previously the date input's iOS-rendered placeholder was invisible in dark mode.
- Dev workflow: `vite.config.ts` now proxies all `/api/*` calls to `VITE_API_URL` (defaults to `localhost:3001`), and the frontend uses an empty `API_BASE` in dev so requests stay same-origin. Makes phone-on-LAN testing work without adding LAN IPs to the production API's CORS allowlist.

## 2026-04-23
- Polish API-key revocation UX — confirmation modal with key name, last-used date, and an explicit warning about consumers getting `401` immediately; "Delete" renamed to "Revoke" throughout; in-flight loading state and inline error display
- Add scope-based authorization for API keys — `read`, `write`, and `ai` scopes; existing keys default to `legacy` (full access) for migration safety; insufficient scope returns 403; `/api/api-keys/*` routes now require Firebase auth (API keys cannot mint or revoke other API keys). Settings UI shows scope checkboxes on create and badges per existing key. **Deploy step:** run `npm --prefix api run db:push` to add the `scopes` column.

## 2026-04-22
- Add API documentation (`Docs/API.md`) and agent integration quickstart (`Docs/AgentQuickstart.md`)
- Add rate limiting to the API — IP-based pre-auth (100 req/min), user-based post-auth (300 req/min), and stricter per-user limit on the Anthropic proxy (30 req/min); standard RFC `RateLimit-*` headers on all responses

## 2026-04-20
- Add design system showcase page at `/design-system.html` with live light/dark and adaptive-energy toggles, linked from Settings → App (#78)

## 2026-04-19
- Convert mobile hamburger menu from dropdown to slide-out panel — full-height panel from right with backdrop, body scroll lock, quick-create buttons, active-state nav links, sign-out action, and grouped navigation sections (#74)
- Move Notes from bottom tab bar to menu panel, replace with "More" button — bottom nav is now Today, Inbox, Backlog, Habits, More (#74)
- Add consistent hamburger menu button to all page headers via reusable MenuButton component (#74)
- Shrink large "New" buttons to icon-only style on Inbox, Backlog, Notes, Tempo AI, Playlists, and Projects pages (#74)
- Make MobileMenu global in AppShell instead of per-page — removed from 9 individual page files (#74)
- Quick wins: fix Backlog hooks order crash, add description field to todos, bump due-today priority to match overdue, add Brain Dump to mobile menu, fix filter dropdown overflow on mobile (#72, #73)

## 2026-04-17
- Fix MCP server to forward client API key to backend — remote server was using a single server-side key for all API calls, causing all clients to operate as the same user. Now validates and forwards the client's key so each account is isolated (#67)

## 2026-04-14
- Fix mobile UX bugs round 2: FilterDropdown portals to escape overflow clipping (Backlog Sort/Project/Energy dropdowns) with viewport-aware flipping, playlists Create works (server: explicit UUIDs for legacy `id text` columns; client: optimistic cache injection so detail page renders immediately), Today header Plan/Focus buttons match hamburger ghost style, `min-h-dvh` for correct iOS PWA viewport, Today chat bar clears safe-area home indicator, TodoDetailDrawer locks body scroll so page doesn't scroll out from under the modal (#66)
- Only show mood on Today page if logged today — filter the mood widget to today's date only (#65)
- Fix Tempo project todos round 1: double completion toast race condition, habits toggle (API date middleware was stringifying JSONB completion keys), Projects menu separator, Today view mobile layout (#64)

## 2026-04-07
- Fix CORS preflight on MCP endpoint so Claude.ai browser OAuth flow completes (#56)
- Add OAuth 2.0 to MCP server — authorization code flow with PKCE, consent page, token refresh, dual-auth (API key for Claude Code + Bearer for Claude.ai) (#55)
- Deploy MCP server as remote Railway service with Streamable HTTP transport — accessible from Claude.ai, Claude mobile, and Claude Code on any device (#54)

## 2026-04-06
- Fix due date 500 error (plain YYYY-MM-DD strings crashed Drizzle) and add 6 playlist MCP tools + energy/time fields to todo tools (#53)
- Fix adaptive theme contrast — dramatically increased color differentiation between energy levels (#52)
- Add P3: completion sparkle on checkbox, adaptive energy theme toggle in Settings, routine playlists with full CRUD and "Start" flow (#51)
- Add P2: momentum streaks (flame indicator), "Just pick for me" AI task picker, smart quick capture with AI metadata suggestions (#50)
- Add guided daily planning ritual — 5-step morning flow: energy check-in → yesterday review → AI suggestions → confirm → time estimates (#49)
- Add granularity slider (1–5) to AI task breakdown — from broad strokes to baby steps (#48)
- Add one-task Focus Mode — full-screen current task with timer, Done/Skip/Break actions, transition breathing space (#47)
- Add time estimates to todos (5/15/25/45/60/90 min pills) and visual timer with dynamic end-time on Today view (#46)
- Update PRD with research-backed ADHD feature roadmap from competitive analysis (#45)
- Fix hashtag extraction and add visual hashtag badges in note editor (#44)
- Add Projects view page and project field for Notes (#43)

## 2026-04-05
- Update app icon to new Figma design (#40)
- Fix: New Todo and New Note create buttons — Drizzle expects Date objects, not ISO strings (#39)
- Migrate chat history from Firestore to Postgres — Firebase is now auth-only (#38)
- Fix: CORS for Railway frontend domain (#37)
- Add CORS middleware and remove dead Firestore context files (#36)
- Add Tempo MCP server with 17 tools for AI integration (#35)
- Move Anthropic proxy from Firebase Cloud Function to Railway API (#34)
- Swap frontend data layer from Firestore to TanStack Query + REST API (#33)

## 2026-04-04
- Add Express API server with Drizzle ORM and Firestore → Postgres migration (#32)

## 2026-04-03
- Improve mobile UX: 44px touch targets, iOS zoom fix, layout polish (#31)

## 2026-04-02
- Chat history persistence with full markdown toolbar (#30)
- Polish todo drawer: bottom actions + auto-focus title (#29)
- Fix bugs, add Completed page, remove FAB for universal hamburger menu (#28)

## 2026-04-01
- Add Anthropic API proxy via Firebase Cloud Function (#27)
- Merge Calendar into Backlog with unified toolbar (#26)
- Clean up desktop sidebar and Backlog filters (#25)

## 2026-03-31
- Add Claude AI: ADHD task breakdown and Today curation (#24)

## 2026-03-30
- Add Calendar View with month grid and event CRUD (#23)
- Add recurring todos and inline todo checkboxes in notes (#22)
- Add Habit Tracker with daily check-ins and contribution grid (#21)
- Add Insights and Weekly Review pages (#20)
- Add settings & profile to mobile header (#19)
- Add mobile Markdown formatting bar above keyboard (#18)
- Add Create Note button to Notes page (#17)
- Add projects as sidebar folders (#16)

## 2026-03-29
- Today view: fixed daily set of 5 todos (#15)
- Add project dropdown with typeahead (#14)
- Fix mobile responsiveness for PWA (#13)
- Add floating formatting toolbar to Notes editor (#12)

## 2026-03-27
- Soft reminders with gentle notifications (#11)
- Global keyboard shortcuts with help sheet (#10)
- Coda CSV import with column mapping and preview (#9)

## 2026-03-26
- PWA assets, offline indicator, and update prompt (#8)
- Note-todo linking with bidirectional navigation (#7)
- Dark mode with Light / Dark / System toggle (#6)
- Backlog project grouping with collapsible sections and sort (#5)
- Inbox triage flow with progress tracking and batch move (#4)
- Completion animation, defer popover, and satisfaction feedback (#3)
- Refactor: shared Firestore subscription context (#2)
- Wire up Firestore, Milkdown editor, and inline todo editing (#1)
