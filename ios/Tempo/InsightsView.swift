import SwiftUI

/// Placeholder — replaced by the phase 4 feature build.
struct InsightsView: View {
    var body: some View {
        Text("Coming soon")
            .foregroundStyle(Theme.onSurfaceVariant)
    }
}
